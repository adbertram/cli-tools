# Scrunch API Credentials
IS_DEFAULT_PROFILE=1

# API Key (required) -- prompted during 'scrunch auth login' and saved automatically.
API_KEY=

# API Base URL (optional - defaults to production)
BASE_URL=https://api.scrunchai.com/v1

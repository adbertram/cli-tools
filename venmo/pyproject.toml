[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "venmo-cli"
version = "0.1.0"
description = "CLI interface for Venmo API"
requires-python = ">=3.11"
dependencies = [
    "typer>=0.9.0",
    "python-dotenv>=1.0.0",
    "requests>=2.31.0",
    "venmo-api>=0.3.1",
    "cli-tools-shared",
]

[project.scripts]
venmo = "venmo_cli.main:app"

[tool.setuptools.packages.find]
where = ["."]
include = ["venmo_cli*"]

[tool.uv.sources]
cli-tools-shared = { path = "../_repo/cli-tools-shared", editable = true }

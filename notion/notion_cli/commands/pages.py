"""Credential metadata for the `pages` command group."""

COMMAND_CREDENTIALS = {
    "search": ["custom"],
    "list": ["custom"],
    "get": ["custom"],
    "create": ["custom"],
    "import": ["custom"],
    "export": ["custom"],
    "duplicate": ["custom"],
    "update": ["custom"],
    "delete": ["custom"],
    "content": ["custom"],
    "blocks": ["custom"],
}

# {{Name}} CLI Wrapper Configuration
# Documentation: {{docs_url}}
IS_DEFAULT_PROFILE=1

# Underlying CLI command (defaults to {{cli_command}})
CLI_COMMAND={{cli_command}}

# Optional: Full path to CLI executable (if not in PATH)
# CLI_PATH=

# BrickLink CLI profile template
ACTIVE=true
BASE_URL=https://api.bricklink.com/api/store/v1

# Configure reusable OAuth credentials with `bricklink auth login -c oauth`.
CLIENT_ID=
CLIENT_SECRET=
ACCESS_TOKEN=
REFRESH_TOKEN=

# Set to true for the active profile.
ACTIVE=true

# Dropbox app credentials. Use `dropbox auth login` to configure real values.
CLIENT_ID=
CLIENT_SECRET=

# OAuth runtime values are managed by the CLI after login.
ACCESS_TOKEN=
REFRESH_TOKEN=
TOKEN_EXPIRES_AT=
ACCOUNT_ID=

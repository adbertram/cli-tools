# hermes-sessions CLI configuration (shape only — never put real secrets here).
# This CLI has no reusable credentials. Reusable human-supplied CLI credentials
# belong in _repo/_secret-manager/secrets.sh, never in any .env file.
ACTIVE=true

# Non-authentication configuration
# Hermes home directory holding state.db. Unset means ~/.hermes.
HERMES_HOME=

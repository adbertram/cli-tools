# Facebook CLI profile configuration
ACTIVE=true

# Each authentication profile uses exactly one auth type:
# AUTH_TYPE=browser_session
# AUTH_TYPE=oauth_authorization_code
AUTH_TYPE=

CLIENT_ID=
CLIENT_SECRET=
REDIRECT_URI=
ACCESS_TOKEN=
REFRESH_TOKEN=
TOKEN_EXPIRES_AT=

GRAPH_VERSION=v24.0

# Notifier CLI Wrapper Configuration
# Documentation: https://github.com/julienXX/terminal-notifier

# Set to 1 for the default profile
ACTIVE=true

# Underlying CLI command (defaults to terminal-notifier)
# NOTIFIER_CLI_COMMAND=terminal-notifier

# Optional: Full path to CLI executable (if not in PATH)
# NOTIFIER_CLI_PATH=

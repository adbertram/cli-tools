[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "roomba-cli"
version = "0.1.0"
description = "CLI interface for Roomba API"
requires-python = ">=3.11"
dependencies = [
    "typer>=0.9.0",
    "python-dotenv>=1.0.0",
    "requests>=2.31.0",
    "pydantic>=2.0.0",
    "roombapy>=1.9.0",
    "cli-tools-shared",
]

[project.scripts]
roomba = "roomba_cli.main:app"

[tool.setuptools.packages.find]
where = ["."]
include = ["roomba_cli*"]

[tool.uv.sources]
cli-tools-shared = { path = "../_repo/cli-tools-shared", editable = true }

# Descript CLI Configuration
# Authentication is extracted from the running Descript app.

IS_DEFAULT_PROFILE=1

# Optional: API Base URL (defaults to production)
# DESCRIPT_BASE_URL=https://web.descript.com

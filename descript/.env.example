# Descript CLI Configuration
# API authentication is managed by the official descript-api CLI.
# Run: descript config set api-key

ACTIVE=true

# Optional: API Base URL (defaults to production)
# DESCRIPT_BASE_URL=https://descriptapi.com/v1

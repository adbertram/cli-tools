[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "ship7-cli"
version = "0.1.0"
description = "CLI interface for Ship7 (browser automation)"
requires-python = ">=3.11"
dependencies = [
    "typer>=0.9.0",
    "python-dotenv>=1.0.0",
    "pydantic>=2.0.0",
    "cli-tools-shared @ git+https://github.com/adbertram/cli-tools.git#subdirectory=cli-tools-shared",
]

[project.scripts]
ship7 = "ship7_cli.main:app"

[tool.setuptools.packages.find]
where = ["."]
include = ["ship7_cli*"]

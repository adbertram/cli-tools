# Apple CLI Configuration

# Browser session is stored in the shared CLI profile directory.
ACTIVE=true
BASE_URL=https://reportaproblem.apple.com

# Headless browser mode for non-login commands. auth login always opens headed.
HEADLESS=true

# Response cache settings
CACHE_ENABLED=true
CACHE_TTL=3600

# PayPal CLI Configuration
# Get credentials from: https://developer.paypal.com/dashboard/applications
IS_DEFAULT_PROFILE=1

# OAuth Credentials
CLIENT_ID=
CLIENT_SECRET=

# OAuth Tokens (populated automatically after login)
ACCESS_TOKEN=
TOKEN_EXPIRES_AT=

# API Base URL (optional - defaults to production)
BASE_URL=https://api-m.paypal.com

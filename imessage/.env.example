# Imessage CLI Wrapper Configuration
# Documentation: 

# Set to 1 for the default profile
ACTIVE=true

# Underlying CLI command (defaults to osascript)
# IMESSAGE_CLI_COMMAND=osascript

# Optional: Full path to CLI executable (if not in PATH)
# IMESSAGE_CLI_PATH=

# Shippo API Credentials
# Get these from: https://docs.goshippo.com/
IS_DEFAULT_PROFILE=1

# API Key authentication
API_KEY=

# API Base URL (optional - defaults to production)
BASE_URL=https://api.goshippo.com

# Default from-address for shipments (optional)
FROM_NAME=
FROM_STREET1=
FROM_CITY=
FROM_STATE=
FROM_ZIP=
FROM_COUNTRY=US
